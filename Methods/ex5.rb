# Make and Model

# Write a method called car that takes two artuments and prints a string with
# the values of both arguments.

def car(make, model)
  puts "#{make} #{model}"
end

car("Toyota", "Corolla")

# Make and Model are passed through as arguments to car, which are then printed.